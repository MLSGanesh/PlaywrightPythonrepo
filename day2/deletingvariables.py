a=100
b=200

print(a)
print(b)

del a #Deleted variable a

print(a) #Name error
print(b) #As the above statement got failed, this step is not got executed

# Basic variables like (int, float, str, bool) can store only single variable at a time.
# Derived Datatypes/Data Structures like (list, set, tuple) can store multiple variables at a time.